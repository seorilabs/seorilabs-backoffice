# Stateful infrastructure. CI deployer에는 PVC mutation 권한을 주지 않는다.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backoffice-backup
  namespace: platform
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 2Gi
